Given that a user has successfully logged in to the Environment Uplift Portal, and is on the home page 

WHEN the user has clicked on the MENU drop down

THEN the user should be able to view  a list of Applications to select from as given in the mock up 

The list should contain the following values 
Access Channel
Cotiviti
Independent Dispute Resolution (IDR)
Shared Accums - Inbound 



export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"  # This loads nvm
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"  # This loads nvm bash_completion