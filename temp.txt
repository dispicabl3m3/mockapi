import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RouterTestingModule } from '@angular/router/testing';
import { ActivatedRoute } from '@angular/router';
import { Angulartics2LaunchByAdobe, RequestBuilder } from 'ui-commons'; // Import your dependencies here

import { TabSectionHeaderComponent } from './tab-section-header.component';

describe('TabSectionHeaderComponent', () => {
  let component: TabSectionHeaderComponent;
  let fixture: ComponentFixture<TabSectionHeaderComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [TabSectionHeaderComponent],
      imports: [RouterTestingModule],
      providers: [
        Angulartics2LaunchByAdobe,
        RequestBuilder,
        {
          provide: ActivatedRoute,
          useValue: {
            snapshot: {
              routeConfig: { path: 'test-path' } // Mock ActivatedRoute snapshot
            }
          }
        }
      ]
    }).compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(TabSectionHeaderComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });

  it('should handle onDropDownSubmit correctly', () => {
    // Mock data for input properties
    component.providerDetails = {};
    component.patientDetails = {
      claimFromDate: '2023-01-01',
      memberId: '123456'
    };

    // Simulate a dropdown submit event
    component.onDropDownSubmit({ currentValue: component.viewAdditionalDetailsOptions[0] });

    // Add your assertions here based on the expected behavior of onDropDownSubmit
    // For example, you can expect that certain methods were called or properties were set correctly.
  });

  // Add more test cases as needed for your component's methods and behavior.
});
